import { CommentsToDisplayPipe } from './comments-to-display.pipe';

describe('CommentsToDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentsToDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
