import { SideBarIconDirective } from './side-bar-icon.directive';

describe('SideBarIconDirective', () => {
  it('should create an instance', () => {
    const directive = new SideBarIconDirective();
    expect(directive).toBeTruthy();
  });
});
